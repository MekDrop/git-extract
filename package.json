{
  "name": "git-extract",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "frontend"
  ],
  "scripts": {
    "dev": "npm -w frontend run dev",
    "build": "npm -w frontend run build",
    "lint": "npm -w frontend run lint"
  },
  "engines": {
    "node": ">=18"
  }
}
